<% if false %>
<div class="modal viewer">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-body">
        <p>
          製品【<%= autopart.car_brand %> | <%= autopart.parts_category %>】を削除します。宜しいでしょうか。
          削除はせずに、出品停止とする場合にはXXXを押してください。
        </p>
      </div>
    </div>
  </div>
</div>
<% end %>

<div class="modal viewer" tabindex="-1" role="dialog">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title"><%= autopart.car_brand %> | <%= autopart.parts_category %>を削除します。</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <p>
          削除した場合、元には戻せません。削除はせずに、出品停止とする場合にはXXXを押してください。
        </p>
        <p>
          削除せずに「出品停止」とする場合は<%= link_to "コチラ", "#" %>から。
        </p>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-dismiss="modal">
          <i class="fas fa-times"></i>
          &nbsp;<strong>キャンセル</<strong>
        </button>
        <button type="button" class="btn btn-danger">
          <i class="far fa-trash-alt"></i>
          &nbsp;<strong>削除する</strong>
        </button>
      </div>
    </div>
  </div>
</div>
